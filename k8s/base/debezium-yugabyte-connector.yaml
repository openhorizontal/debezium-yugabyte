apiVersion: kafka.strimzi.io/v1
kind: KafkaConnector
metadata:
  name: CONNECTOR_NAME
  labels:
    strimzi.io/cluster: yugabyte-cluster
spec:
  class: io.debezium.connector.postgresql.YugabyteDBConnector
  tasksMax: 1
  config:
    database.hostname: yb-tservers.yb-dev.svc.cluster.local
    database.port: 5433

    # Unlike yboutput, pgoutput is unaware of the internal CHANGE identity format of YugabyteDB and rejects it.
    plugin.name: yboutput
    publication.autocreate.mode: filtered

    snapshot.mode: never

    key.converter: org.apache.kafka.connect.json.JsonConverter
    key.converter.schemas.enable: 'false'

    # PGCompatible unwraps the {value, set} columns of yboutput so EventRouter sees the envelope (like pgoutput)
    transforms: pgcompat,outbox
    transforms.pgcompat.type: io.debezium.connector.postgresql.transforms.yugabytedb.PGCompatible
    transforms.outbox.type: io.debezium.transforms.outbox.EventRouter
    transforms.outbox.table.fields.additional.placement: type:header:eventType

    value.converter: io.debezium.converters.BinaryDataConverter
    value.converter.delegate.converter.type: org.apache.kafka.connect.json.JsonConverter
    value.converter.delegate.converter.type.schemas.enable: 'false'
